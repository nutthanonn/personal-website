import React from "react";
import Box from "@mui/material/Box";
import CodeIcon from "../../images/codeIcons";

const TitleImage: React.FC = () => {
  return (
    <Box>
      <CodeIcon />
    </Box>
  );
};

export default TitleImage;
